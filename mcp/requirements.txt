# vla-ft MCP server runtime deps. Install into a dedicated venv:
#   python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
# The server also imports the verified core from ../containers/vla-ft/ (added to sys.path
# by vla_aws.py) — no packaging needed, single source of truth.
mcp>=1.2
boto3>=1.34
